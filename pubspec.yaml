name: webmcp_flutter
description: A Flutter tool registry with widget lifecycle, optional page semantics, and experimental Chrome WebMCP publication.
version: 0.1.8
repository: https://github.com/ortal83cohen/flutter_webmcp
homepage: https://github.com/ortal83cohen/flutter_webmcp
topics:
  - webmcp
  - mcp
  - flutter-web
  - ai-agents
  - flutter

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1

dev_dependencies:
  analyzer: ^13.3.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.26.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

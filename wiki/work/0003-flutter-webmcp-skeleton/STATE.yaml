id: "0003-flutter-webmcp-skeleton"
title: "Flutter WebMCP package skeleton (plan rewrite of 0002-flutter-webmcp-stack)"
route: full
phase: superseded
round: 3
verdict: FAIL
owner_agent: null
created: "2026-09-05"
updated: "2026-09-05"

blockers: []

followups:
  - summary: "Line-by-line capability diff of webmcp_pilot against KickNext/flutter_webmcp v0.3.0 and intentcall_webmcp; prerequisite for the publishing work item, not for the skeleton."
    severity: IMPORTANT
    owner: "unassigned"
  - summary: "Confirm pub.dev availability of the name webmcp_pilot (plan step 1); fallback webmcp_autopilot is pre-decided."
    severity: IMPORTANT
    owner: "unassigned"

decisions:
  - round: 2
    verdict: CONDITIONAL
    action: revise-plan
    reason: "plan-review-02: CONDITIONAL, 1 blocker + 6 IMPORTANT + 3 NIT, no oscillation (round 1's blockers verified independently closed; two cells drew a second, different finding each, flagged as ones to watch but not oscillating). Fixed: F-001 AC-006 compared the wrong pair of runs (run-vs-run instead of committed-vs-after-first-run), which is invisible to an idempotent migration that stabilizes after firing once — rewrote to compare the committed blob's checksum against the checksum after exactly one run; F-002 AC-002's negative predicted failure at stage 1 (wiki lint) which only needs python3, present even under the minimal PATH — corrected to name stage 2 (dependencies); F-003 added AC-033 for the .gitignore entries step 1 writes; F-004 unified the secret-scan scope (all tracked files, any extension) vs the import-scan scope (Dart files only, since only Dart can import) which had been stated three inconsistent ways; F-005 fixed the full-suite command to the named literal `bash tools/check.sh` everywhere AC-025 compares it; F-006 added explicit push-authorization language to step 10, naming this repository's standing explicit-consent-per-push rule rather than assuming pre-authorization; F-007 removed the unused, uncriterioned document_model_context.dart interop-extension file entirely — the presence check reaches dart:js_interop_unsafe directly by string key and needs no typed getter until a later work item calls the real API, closing the gap rather than adding a criterion for dead code; F-008/009/010 NITs closed (step 4's completion condition scoped to lib/ only since step 5's source-text test is expected to name the same file; the repository-field justification corrected to state the real reason - a publishing-time decision, not a wrong URL; format stage extended to cover example/test/ alongside the other three source paths, matching the analysis stage's scope)."
  - round: 1
    verdict: FAIL
    action: revise-plan
    reason: "plan-review-01: FAIL, 2 blockers + 12 IMPORTANT/NIT. Root cause of F-001 traced to Flutter SDK source (flutter_tools/lib/src/migrations/analysis_options_migration.dart): AnalysisOptionsMigration is idempotent, only rewriting analysis_options.yaml when its analyzer.exclude list is missing any of 7 fixed entries (build/**, android/**, ios/**, web/**, windows/**, macos/**, linux/**) — confirmed by reading the source directly. Fix: commit both options files pre-migrated with the complete list from step 2 onward, which neutralizes the mutation for every Flutter command (not just the analyzer binary choice, which was the wrong axis). Also fixed: F-002 removed the stale round-4 risk row (resolved by opening this new folder); F-003 settled the package name as verified fact (checked pub.dev directly, HTTP 404, confirmed unclaimed) rather than a deferred runtime step, closing the frozen-criteria naming risk; F-004 fixed step 1's contradictory completion condition and false stable-channel premise; F-005 fixed AC-002 to use a fixed minimal PATH instead of stripping one of three ambiguous flutter-providing directories; F-006 removed the wrong repository/homepage field (this repo's origin resolves to an unrelated scaffold repo) rather than assert a fabricated or wrong URL; F-007 fixed AC-005 to actually exercise an info-severity diagnostic, not just a warning; F-008 fixed AC-027/028 to check the working tree directly rather than git ls-files (which reports the index and misses unstaged renames); F-009 added AC-021 and a new test file checking transport_web.dart's identifier/log strings via text inspection, since no criterion previously touched it; F-010 added timing measurement to the final step; F-011 removed the requirement to push a deliberately-broken branch for AC-026 (now checked by reading a prior completed run's log instead); F-012 split AC-017's negative case into two independently-checked mutations so the src/-import-prohibition half is actually exercised. Step count dropped from 12 to 11 (name-settling step folded into Approach as settled fact); all step/criterion cross-references renumbered accordingly."
  - round: 0
    verdict: null
    action: revise-plan
    reason: "This work item supersedes 0002-flutter-webmcp-stack. 0002's plan phase hit the 3-round validation cap with an explicit oscillation verdict (plan-review-03.md); tools/lint_wiki.py's check_reviews rejects a fourth round in the same validation/ folder by design (round cap is enforced per work-item folder, and only research|plan|impl review prefixes are accepted — there is no in-place reset mechanism). Per wiki/conventions/naming.md, work-item numbers are never reused and are superseded rather than renumbered, so the plan rewrite (user-approved after the oscillation escalation) starts fresh here as 0003. 00-research.md (already PASSed validation round 2 in 0002, 0 blockers) is carried forward unchanged. 01-plan.md and 02-criteria.md are the from-scratch rewrite, written with every defect that oscillated across 0002's three rounds as an explicit hard constraint (analyzer binary decided as dart analyze, every criterion's negative case swept for genuine injectability, CI-independence check made literally constructible, AC-004-equivalent given a named test artifact, rollback blast-radius corrected, example manifest given publish_to: none, toolchain pin made concrete). Validation restarts at round 1 for both phases in this folder."

escalations:
  - date: "2026-09-05"
    reason: oscillating-loop
    question: "plan-review-03 (round 3 of this folder's 3-round cap) FAILed on 2 blockers, both recurrences of round-2 findings, so the validator flagged oscillation per wiki/conventions/validation-rubrics.md and recommended escalation over a 4th round in this folder. Unlike the prior escalation (0002's round-3 oscillation, a genuine design defect requiring a full rewrite), this recurrence is a propagation miss, not a design flaw: round 2 correctly fixed AC-006's criterion (compare committed-checksum vs. after-first-run, not run-vs-run) and the format stage's path list (four paths, not three), but the corresponding prose in 01-plan.md's step 2, step 6, and the Verification approach section still describes the old run-vs-run comparison verbatim (including the exact disproven phrase 'the only proof that matters'), and the Documented-commands paragraph still says 'the three source paths' with no list of three ever given. The validator independently re-verified every other mechanism in the plan as sound (the analyzer-migration fix itself, the PATH/preflight check, the removed interop file, the bash tools/check.sh literal, all criteria's negative cases) — this is confined to two unpropagated edits. Also raised (IMPORTANT, not blocking): AC-031's wall-time budget has no step that can fail it; LICENSE's copyright holder is still unnamed with no step gating on it; AC-026's push-authorization language attributes a 'standing repository rule' that no repository file actually states. Question put to the user: given this is a propagation-of-an-already-correct-fix issue rather than a design defect, should this proceed as a new work item 0004 carrying forward the same (validated-sound) design with these specific text fixes applied precisely this time — rather than a full rewrite from scratch, which the design does not need?"
  - date: "2026-09-05"
    reason: scope-change
    question: "0002-flutter-webmcp-stack's plan phase hit the 3-round validation cap with an oscillation verdict. Resolved without further human input (mechanical consequence of the already-approved rewrite decision, not a new judgment call): opened this new work-item folder 0003-flutter-webmcp-skeleton rather than reusing 0002's number, per wiki/conventions/naming.md ('work items: numbers never reused, even for abandoned items') and because tools/lint_wiki.py hard-caps validation rounds per folder with no reset mechanism. 0002-flutter-webmcp-stack is left in place, untouched, as the historical record of the research work and the failed plan attempts; it is not deleted per the wiki's never-delete rule. See 0002's STATE.yaml for the reciprocal note."

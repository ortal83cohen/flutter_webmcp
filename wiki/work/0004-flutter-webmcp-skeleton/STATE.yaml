id: "0004-flutter-webmcp-skeleton"
title: "Flutter WebMCP package skeleton (propagation fix of 0003-flutter-webmcp-skeleton)"
route: full
phase: done
round: 4
verdict: PASS
owner_agent: "Codex"
created: "2026-09-05"
updated: "2026-09-10"

blockers: []

followups:
  - summary: "Line-by-line capability diff of webmcp_pilot against KickNext/flutter_webmcp v0.3.0 and intentcall_webmcp; prerequisite for the publishing work item, not for the skeleton."
    severity: IMPORTANT
    owner: "unassigned"

decisions:
  - round: 4
    verdict: PASS
    action: retain-verbatim-evidence-whitespace
    reason: "After tracking the append-only validator reports, default cached diff whitespace checking reports trailing spaces and space-before-tab only inside pasted command transcripts. Retain those exact outputs as required evidence rather than rewrite historical reports. Strict cached diff checking passes for all other files; report-only checking passes with just those two transcript whitespace rules disabled. This is an accepted evidence-format artifact, not a source/test defect. Final commands and disposition are appended to 11-completion.md."
  - round: 4
    verdict: PASS
    action: complete-local-work
    reason: "All implementation groups and all 46 criteria pass independent impl-review-04. Product contract, ADR 0003 and direct router links are complete. Documentation and final source corrections are delivered together in the working tree; no additional commit or push is required or performed by this orchestration. A concurrent user/external commit is preserved. Live CI remains explicitly permitted pending under AC-026, and browser publication remains outside scope. Completion evidence is indexed in 11-completion.md."
  - round: 4
    verdict: PASS
    action: document
    reason: "impl-review-04 independently passes all 46 criteria with no findings. Official AST parsing resolves the recurring boundary defect; full suite exits zero in 31.15 seconds with 39 root tests, one example test and web build. Proceed to product contract, ADR and router documentation. AC-026 remote run is explicitly permitted pending; local workflow verified."
  - round: 3
    verdict: FAIL
    action: official-parser-implemented
    reason: "The official analyzer AST replaces all handwritten import parsing. Analyzer is a direct development dependency only; runtime files and marker scanner remain unchanged. Eighteen valid forbidden imports compile independently and fail hygiene checks with decoded URI diagnostics. Full suite exits zero with 39 root tests, one example test and Stage 6 passed. Re-enter independent verification for the changed parser artifact; 10-parser-repair-evidence.md records implementation evidence."
  - round: 4
    verdict: PASS
    action: implement-parser-replacement
    reason: "plan-review-04 independently accepts 09-parser-plan-amendment.md with no findings and clean lint. Replace manual parsing with the official analyzer AST using a direct development dependency; preserve exact marker behavior, runtime code and frozen criteria. This removes the demonstrated language-semantics mismatch rather than extending it."
  - round: 3
    verdict: FAIL
    action: replace-handwritten-parser
    reason: "impl-review-03 F-001 proves another legal import string form bypasses handwritten parsing; its recurrence assessment explicitly says no oscillation and confirms previous fixes remain intact. Re-enter plan to replace manual language parsing with the official analyzer AST via a direct development dependency, rather than patch another string form. All public behavior and frozen criteria remain unchanged. Preserve the full prior reports."
  - round: 2
    verdict: FAIL
    action: escape-repair-verified-locally
    reason: "String decoding now handles fixed/braced Unicode, hex and standard escapes while preserving raw strings. Six isolated valid encoded import directives are rejected with decoded URI diagnostics. Full suite reports 39 root tests, one example test and successful web build; 08-escape-repair.md records evidence. Re-enter verify for one independent review of this reworked artifact; reuse unchanged independent evidence without repeating the 400-second timing probe."
  - round: 2
    verdict: FAIL
    action: repair-string-decoding
    reason: "impl-review-02 independently resolved prior F-001 and F-003 and ordinary multiline imports, but found valid Unicode-escaped import URIs bypass the scanner (AC-018/AC-044). This is a new concrete string-decoding defect, not an opposing requirement or plan change. Re-enter implementation to decode valid Dart string escapes while retaining raw-string semantics, then independently verify the changed artifact. Preserve both prior FAIL reports."
  - round: 1
    verdict: FAIL
    action: implementation-reworked
    reason: "F-001 now explicitly names WebMcpToolHandler. F-002 is addressed by full-source import token recognition across comments/newlines and conditional URIs. F-003 was routed to plan, independently clarified, then implemented with exhaustive fixed markers. Full suite reports 35 root tests and one example test passing with successful web build; isolated repair probes are recorded in 07-repair-evidence.md. Re-enter verify for one independent review of the reworked artifact (impl-review-02), preserving impl-review-01 and its original FAIL."
  - round: 3
    verdict: PASS
    action: implement-amendment
    reason: "plan-review-03 independently accepted 06-plan-amendment.md with no findings and clean wiki lint. Implement its exhaustive marker set and legal multiline/comment-separated import recognition, retaining all frozen criteria and prior evidence. F-001 explicit handler reference is already restored and awaits final independent verification."
  - round: 1
    verdict: FAIL
    action: route-by-defect
    reason: "impl-review-01 F-001 and F-002 belong to implementation: restore the explicit handler typedef reference and recognize legal multiline import directives. F-003 belongs to the plan: AC-032 refers to an unspecified literal marker set. Re-enter plan only to define that normative set in append-only 06-plan-amendment.md, independently validate that clarification, then repair implementation. Frozen criteria IDs and text remain unchanged; no code patch is used to conceal the plan defect."
  - round: 0
    verdict: null
    action: fix-directly
    reason: "Pre-validation execution found action disposal using an updated name instead of the mounted name, mutable action schema storage, missing package-internal import detection, and a callback regression test that did not distinguish immutable closures. Implementer corrected all four; focused checks and exact negative mutations were rerun. Evidence: 05-negative-checks.md, including retained initial failures and superseding reruns. No frozen criterion changed."
  - round: 0
    verdict: null
    action: track-new-files
    reason: "The authorized end-to-end plan explicitly requires tracked new package files in AC-028. Added only newly created package paths to the actual index as a reversible tracking step; preserved pre-existing unstaged modifications and made no commit or push."
  - round: 0
    verdict: null
    action: implement
    reason: "Declare package:test as a direct development dependency for AC-045 scope tests, avoiding a dependency on flutter_test or an undeclared transitive import. This is an implementation dependency needed by the frozen criterion; it changes no public interface or acceptance criterion."
  - round: 2
    verdict: PASS
    action: implement
    reason: "User authorized end-to-end implementation on 2026-09-10. Resume the approved plan with its recorded findings disposition; freeze criteria without changing their substance. Preserve all pre-existing dirty changes. Perform destructive negative probes in an isolated temporary snapshot. The user supplied the exact MIT copyright holder flutter_webmcp in this session; LICENSE is authorized. Live CI push remains pending explicit authorization."
  - round: 2
    verdict: PASS
    action: annotate-only
    reason: "plan-review-02: PASS, 0 blockers, 5 IMPORTANT + 3 NIT. Per explicit user instruction, stopped the multi-round validator-agent cycle here (no round 3 spawned) and fixed the 5 IMPORTANT findings directly in a single pass rather than re-running a validator: F-001 WebMcpTransport's declaring file was misattributed to step 3 (webmcp_exceptions.dart) when it's actually written in step 4 (lib/src/transport/webmcp_transport.dart) - corrected step 3's wording and analysis-error accounting; F-002 03-tasks.md had several AC citations on tasks that didn't own the governing file (AC-020's call site is lib/src/webmcp.dart per task 2.1, not the transport files per 2.2; AC-008 belongs to task 4.1's example/pubspec.yaml, not 1.2's root manifest; AC-025 spans both 6.1's AGENTS.md and 6.2's workflow file; AC-028 is cross-cutting and moved to 7.1's final-verification task rather than any single content task) - all reassigned to their correct owners; F-003 AC-028 required LICENSE unconditionally with no pending clause even though step 9 gates LICENSE on a human decision - added the same pending-not-failing treatment AC-026 already has; F-004 step 5's hygiene-test description referenced example/lib/ before step 6 creates it - clarified the scan tolerates a not-yet-existing directory as zero files and only actually runs at full-suite time; F-005 'the verification artifact' and 'the implementation notes' were referenced repeatedly with no defined location - anchored both as sections of one new file, wiki/work/0004-flutter-webmcp-skeleton/04-notes.md, defined once at the top of the Steps section. Per user instruction, the plan phase stops here as PASS without further validator rounds and without proceeding to implementation."
  - round: 1
    verdict: CONDITIONAL
    action: revise-plan
    reason: "plan-review-01: CONDITIONAL, 1 blocker + 6 IMPORTANT + 2 NIT, no oscillation (first round in this folder). Fixed: F-001 Rollback section contradicted step 10/AC-026 by claiming the CI push was required — corrected to state it's conditional on separate human authorization; F-002 the plan claimed .dart_tool was excluded by explicit entries but no step wrote them (relied on the analyzer's hidden-directory default instead) — added .dart_tool/** as a written entry in both options files (9 entries root, 8 example); F-003 unified the forbidden-import scan scope (lib/, example/lib/ only, since only those hold Dart source) vs the secret-token scan's wider 4-directory scope, which step 5 had stated inconsistently from the Interfaces section; F-004 step 3's 'zero issues' completion condition was unmeetable since the transport seam it references doesn't exist until step 4 — rescoped to allow only transport-seam-shaped unresolved-reference errors at that point; F-005 pinned the Flutter version literal concretely as 3.47.0 (the actual installed/verified toolchain version) instead of leaving it undecided while step 2 depended on it; F-006 added 03-tasks.md (11 sequential tasks across 7 groups, no [P] markers since the plan has no parallelism, each citing its satisfied criteria); F-007 decoupled step 9's README/AGENTS.md/CHANGELOG edits from the LICENSE copyright-holder gate, so an unanswered licensing question no longer stalls three unrelated criteria; F-008/F-009 NITs closed (checksum wording no longer says 'as committed' before anything is committed; example's exclusion-list entry count and its relationship to the root's extra example/build/** entry spelled out precisely)."
  - round: 0
    verdict: null
    action: revise-plan
    reason: "This work item supersedes 0003-flutter-webmcp-skeleton. 0003's plan phase hit the 3-round validation cap on round 3 with 2 blockers, both flagged as oscillating by the validator, but the validator's own report and STATE.yaml's escalation note establish these were a propagation miss (round 2 correctly fixed AC-006's criterion and the format stage's path scope, but the matching prose in 01-plan.md's step 2, step 6, and Verification approach, and in the Documented-commands paragraph, was never updated to match) rather than a genuine design defect — every other mechanism in the plan (the analyzer-migration fix itself, the PATH/preflight check, the removed interop file, the bash tools/check.sh literal, all 33 criteria's negative cases) was independently re-verified as sound in round 3. User consulted and chose: open this new work item carrying forward the same validated design, with the two specific propagation fixes applied precisely, rather than a full rewrite from scratch. 00-research.md (unchanged, already PASSed) and 01-plan.md/02-criteria.md (carried forward from 0003's round-3 state, now being corrected in this folder before its own round 1) are copied in. Validation restarts at round 1 for the plan phase in this folder."

escalations:
  - date: "2026-09-05"
    reason: scope-change
    question: "RESOLVED. 0003-flutter-webmcp-skeleton's plan phase hit the 3-round cap with an oscillation verdict on round 3 (2 recurring findings, both propagation misses per the analysis above). Resolved without further human input beyond the prior consultation: opened this new work-item folder 0004-flutter-webmcp-skeleton per wiki/conventions/naming.md ('work items: numbers never reused, even for abandoned items') and because tools/lint_wiki.py hard-caps validation rounds per folder with no reset mechanism. 0003-flutter-webmcp-skeleton is left in place, untouched, as the historical record; not deleted per the wiki's never-delete rule."

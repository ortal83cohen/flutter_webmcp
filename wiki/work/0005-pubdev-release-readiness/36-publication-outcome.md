# Publication outcome

webmcp_flutter 0.1.0 is published at https://pub.dev/packages/webmcp_flutter/versions/0.1.0. The complete publication plan is not yet closed. This outcome supersedes the preupload status in 32 and33 without removing their historical evidence. Corrected live-command and inclusion rules are in34.

## Verified release

- The normal interactive publisher returned successful upload, exit0. Full output is in35.
- Hosted archive SHA-256: f4996fb8012bd50c075e9d21dc74206cbad24d6a5c1517d0647d6bdda707af83.
- Every hosted file matches the22 approved paths, exact sizes and SHA-256 values in27; inventory SHA-256 remains d73593ced4a5b3b75e9fc77674bf25eabd51e1eadff347e18c50e180932c9f0e.
- A fresh hosted-only consumer resolved exact0.1.0 in an empty cache and built the README sample for web. A separate independent hosted consumer also built successfully.
- Hosted README, Example and Changelog pages return HTTP200 with expected content.

## Open external gate

Independent impl-review-03 reports FAIL limited to AC-010 at2026-09-10T14:06:35Z: pub.dev still says analysis pending, no web platform badge is rendered, and https://pub.dev/documentation/webmcp_flutter/0.1.0/ returns HTTP404. This is an observed processing limitation, not evidence of a source regression or permanent service failure. Exact commands and outputs are retained in35 and the independent report.

The main agent accepts this as an unresolved external verification gate and keeps STATE in verify with a followup. Do not mark the entire work item done. Recheck those two presentation results after the service processes the package; a future successful check requires new append-only evidence. No source change, version overwrite or retraction is justified by the pending state. No recurring automation was created.

## Preservation

Release agents did not stage, commit, push, reset or switch branches. Concurrent Git/index changes were observed without actor attribution; current state was preserved. Published source bytes and MIT LICENSE remain the approved bytes. Credentials and private account identity were not written into release artifacts.

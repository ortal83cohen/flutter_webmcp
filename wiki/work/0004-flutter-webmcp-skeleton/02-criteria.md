# Acceptance criteria: Flutter WebMCP package skeleton

## Frozen

- Frozen at: 2026-09-10
- Frozen by: Codex orchestrator at implementation start

## Criteria

Every negative case below is a transient mutation applied to a working tree on which the criterion's check already passes. Apply one at a time, observe the stated failure, then revert so that the working tree reports no modifications before the next mutation is applied.

| ID | Criterion | How it is checked | Negative case |
|---|---|---|---|
| AC-001 | When `tools/check.sh` is run on an unmodified checkout with the pinned Flutter SDK installed, it shall exit zero and print one preflight line followed by six stage lines in the order wiki lint, dependencies, format, analysis, tests, build. | Run the script from the repository root and from a different working directory; read the exit status and the printed lines. | Delete the wiki-lint stage from the script. Re-run: only five stage lines print and the fixed order asserted by the criterion is absent, so the check fails. Restore the script. |
| AC-002 | When the shell's executable search path is set to a fixed minimal value containing neither the `flutter` nor the `dart` binary (for example `/usr/bin:/bin`, which on this machine's toolchain layout resolves neither name but does resolve `python3`), `tools/check.sh` shall exit non-zero with a message naming the missing binary, before any stage line is printed. | Invoke the script once normally (preflight line prints, stages run), then invoke it with `PATH` replaced by the fixed minimal value and read the message and exit status. | Remove the preflight block from the script and invoke it with the replaced `PATH`. Stage 1 (wiki lint) needs only `python3`, which the minimal `PATH` still resolves, so stage 1 now passes and prints its line; the run instead reaches stage 2 (dependencies, which needs `flutter`) and fails there with a shell "command not found" message instead of the preflight's named message. Either way the named preflight message never appears before a stage line, so the check fails. Restore the script. |
| AC-003 | When any stage's underlying command exits non-zero, `tools/check.sh` shall exit non-zero, print a line naming that stage as failed, and not execute any later stage. | Invert one assertion in `test/registry_test.dart`, run the script, confirm exit status is non-zero, the tests stage is named as failed, and no build stage line is printed. Revert the assertion. | With that inverted assertion still in place, remove the strict-mode setting and the explicit failure check from the script's test stage. The run now exits zero and prints a build stage line, so the check fails. Restore both files. |
| AC-004 | When a Dart file under `lib/`, `test/`, `example/lib/` or `example/test/` is not in formatter output shape, the format stage shall exit non-zero and name that file. | Run the format stage on the unmodified tree and confirm exit zero and no file named. | Add three consecutive blank lines and a misindented statement inside a method in `lib/src/webmcp.dart`. Re-run the format stage: it exits non-zero and names that file, so the passing check fails. Revert the file. |
| AC-005 | When the analysis stage runs its single `dart analyze` invocation from the repository root with infos and warnings fatal, it shall report zero issues for the unmodified tree; it shall report a warning-severity issue located under `example/lib/` when one exists there; and it shall report an info-severity issue located under `lib/` when one exists there. | Run the analysis stage on the unmodified tree; confirm exit zero and a zero-issue report. | Apply two mutations together: declare an unused local variable in `example/lib/main.dart` (a warning), and add a public method with no documentation comment to a class in `lib/src/webmcp.dart` (an info under the enabled documentation rule). Re-run the analysis stage: it exits non-zero and reports both, one under `example/lib/` and one under `lib/`, proving the one invocation reaches the nested package and that infos, not only warnings, are fatal. Revert both files. |
| AC-006 | On a tree freshly checked out from the commit and never yet touched by any Flutter command, each of `analysis_options.yaml` and `example/analysis_options.yaml` shall have the same checksum after the dependency stage's first run as it had in the commit itself, before that run. | Compute each file's checksum from the committed blob (for example `git show HEAD:analysis_options.yaml \| shasum`) before running anything; run the dependency stage once; recompute each file's checksum; compare committed-checksum to after-first-run-checksum. | Remove one required entry (for example `ios/**`) from the committed `analyzer.exclude` list in `analysis_options.yaml`, then run the dependency stage once. `AnalysisOptionsMigration` finds the list incomplete, prints its upgrade notice, and re-adds the missing entry — the file's checksum after this first run differs from its committed checksum, so the check fails. (Running the stage a second time afterward would show no further change, which is why the comparison must be committed-state versus after-first-run, never run-versus-run — a mutation that fires once and then stabilizes is invisible to a run-versus-run comparison.) Restore the original list. |
| AC-007 | When a Dart file containing an analyzer error exists under `example/build/`, the analysis stage shall still exit zero and shall not report any issue with a path under `example/build/`. | Create a transient Dart file under `example/build/` referencing an undeclared identifier, run the analysis stage, confirm exit zero and no issue naming that path, then delete the file. | With that transient file still present, delete the build-directory exclusion entry from `example/analysis_options.yaml`. Re-run the analysis stage: it exits non-zero and names the file under `example/build/`, so the check fails. Restore the options file and delete the transient file. |
| AC-008 | When the analysis stage runs, it shall report no invalid-dependency issue for `example/pubspec.yaml`. | Run the analysis stage on the unmodified tree and read the issue list. | Delete the `publish_to` entry from `example/pubspec.yaml`. Re-run the analysis stage: it exits non-zero with an invalid-dependency issue naming the relative path dependency, so the check fails. Restore the manifest. |
| AC-009 | When a valid tool is passed to `registerTool`, the `tools` listing shall subsequently contain exactly one entry whose name equals that tool's name, and the listing shall be sorted ascending by name when several tools are registered. | Run the test stage; `test/registry_test.dart` registers three tools in non-alphabetical order and asserts the returned names and their order. | Delete the map-insertion statement from `registerTool` in `lib/src/webmcp.dart`. Re-run the test stage: the listing assertions fail, so the check fails. Restore the file. |
| AC-010 | When a tool source returning two tools is passed to `registerSource`, both tools shall appear in the listing, and a source returning a tool whose name is already registered shall cause `WebMcpDuplicateToolException` to be thrown. | Run the test stage; `test/registry_test.dart` covers both a two-tool source and a source colliding with an existing registration. | Change `registerSource` in `lib/src/webmcp.dart` to write each returned tool straight into the map instead of routing through the validating registration path. Re-run the test stage: the collision case no longer throws and the assertion fails, so the check fails. Restore the file. |
| AC-011 | When `registerTool` is called with a name already registered, it shall throw `WebMcpDuplicateToolException` naming that tool, and the first registration's handler shall remain the one reachable through `invokeTool`. | Run the test stage; `test/registry_test.dart` registers two tools with the same name and different handlers, asserts the thrown type and name, then asserts the value returned by invoking that name comes from the first handler. | Delete the duplicate check from `registerTool` in `lib/src/webmcp.dart` so a second registration overwrites the first. Re-run the test stage: no exception is thrown and the invoked handler returns the second handler's value, so the check fails. Restore the file. |
| AC-012 | When `registerTool` is called with a name that is empty, longer than 128 characters, or contains a character outside ASCII letters, digits, underscore, hyphen and period, it shall throw `WebMcpInvalidToolNameException` and the `tools` listing shall stay unchanged. | Run the test stage; `test/registry_test.dart` covers an empty name, a 129-character name, a name containing a space, and a 128-character name that is accepted. | Widen the name pattern in `lib/src/webmcp.dart` to accept any non-empty string. Re-run the test stage: the space and over-length cases no longer throw, so the check fails. Restore the file. |
| AC-013 | When `invokeTool` is called with a registered name and an argument map, it shall await that tool's handler, pass the same argument map through, and complete with the handler's return value. | Run the test stage; `test/registry_test.dart` registers a handler that echoes one argument value back and asserts the returned value equals the value passed in. | Change `invokeTool` in `lib/src/webmcp.dart` to pass an empty map to the handler. Re-run the test stage: the echoed value assertion fails, so the check fails. Restore the file. |
| AC-014 | When `invokeTool` is called with a name that is not registered, it shall complete with a `WebMcpToolNotFoundException` naming that name, and no registered handler shall have been called. | Run the test stage; `test/registry_test.dart` registers one tool whose handler increments a counter, invokes an unregistered name, asserts the thrown type and name, and asserts the counter is still zero. | Change `invokeTool` in `lib/src/webmcp.dart` to fall back to the first registered tool when the requested name is absent. Re-run the test stage: nothing is thrown and the counter reaches one, so both assertions fail. Restore the file. |
| AC-015 | When `unregisterTool` is called twice with the same name, the first call shall return true and remove the tool from the listing, and the second call shall return false, throw nothing, and leave the listing unchanged. | Run the test stage; `test/registry_test.dart` registers one tool, calls unregister twice, asserts true then false, asserts no throw on the second call, and asserts the listing is empty after both calls. | Change `unregisterTool` in `lib/src/webmcp.dart` to throw `WebMcpToolNotFoundException` when the name is absent. Re-run the test stage: the second call throws and the assertion of a false return value fails, so the check fails. Restore the file. |
| AC-016 | When `reset` is called with no argument after tools have been registered and a fake transport installed, the listing shall be empty and `transportId` shall return the default identifier for the running platform; when `reset` is called with a transport, `transportId` shall return that transport's identifier. | Run the test stage; `test/transport_selection_test.dart` covers both forms of reset and reads the identifier after each. | Change `reset` in `lib/src/webmcp.dart` to clear the tool map but keep the currently installed transport. Re-run the test stage: after the argument-less reset the identifier is still the fake transport's, so the check fails. Restore the file. |
| AC-017 | When `test/webmcp_public_surface_test.dart` is read, its import list shall contain only the package's single public library and the test framework, with no import naming any path under `lib/src/` and no relative import into `lib/`; the file shall exercise all seven public registry operations; and it shall name each of `WebMcpScope`, `WebMcpScreen`, `WebMcpAction`, `WebMcpTool`, `WebMcpToolHandler`, `WebMcpToolSource`, `WebMcpTransport` and all five exception types at least once. | Read the file's import lines, its test names and the type names it references; run the test stage and confirm it passes, including the hygiene test's scan of this specific file for a `lib/src/` import. | Apply three separate mutations, each checked in isolation: (a) delete the export of the exception types from `lib/webmcp_pilot.dart` — the public-surface test no longer compiles because the exception types are unresolved, so the suite fails; restore the library file. (b) delete the export of `WebMcpAction` from `lib/webmcp_pilot.dart` — the public-surface test no longer compiles because that type is unresolved, so the suite fails; restore the line. (c) add an import naming a path under `lib/src/` to `test/webmcp_public_surface_test.dart` — `test/repo_hygiene_test.dart`'s scan of that specific file reports the forbidden import and fails, even though the file still compiles and its own assertions still pass; remove the import. |
| AC-018 | When any Dart file under `lib/` or `example/lib/` imports `dart:html`, `dart:js`, `package:js`, `dart:io` or `package:http`, the test stage shall fail and name the offending file and import. | Run the test stage on the unmodified tree; `test/repo_hygiene_test.dart` passes and reports zero offending files. | Add an import of `dart:html` to `example/lib/example_tools.dart`. Re-run the test stage: the hygiene test fails naming that file and that import, so the check fails. Remove the import. |
| AC-019 | When the test suite runs on the Dart virtual machine, `WebMcp.instance.transportId` shall return the non-web transport identifier and no browser interop file shall be loaded. | Run the test stage; `test/transport_selection_test.dart` asserts the identifier equals the non-web value on a fresh singleton. | Swap the two branches of the conditional export in `lib/src/transport/transport_selector.dart` so the default branch names the web implementation. Re-run the test stage: the suite fails to compile on the virtual machine because the web-only libraries are unavailable, so the check fails. Restore the selector. |
| AC-020 | When a fake transport is installed through `reset` and a tool is registered and then unregistered, the transport shall have received exactly two notifications, the registration one first with the tool's name and the unregistration one second with the same name. | Run the test stage; the fake transport in `test/transport_selection_test.dart` appends each notification to a list, which the test asserts against the expected two-element sequence. | Delete the transport notification call from `unregisterTool` in `lib/src/webmcp.dart`. Re-run the test stage: the recorded list has one element and the sequence assertion fails, so the check fails. Restore the file. |
| AC-021 | When `lib/src/transport/transport_web.dart` is read as text, it shall contain the exact identifier string `web-detection` and the exact substring `not published`. | Run the test stage; `test/transport_web_source_test.dart` reads the file's source text and asserts both strings are present, without executing the file. | Change the identifier string literal in `lib/src/transport/transport_web.dart` to a different value. Re-run the test stage: the source-text assertion for `web-detection` fails, so the check fails. Restore the string. |
| AC-022 | When the example's registration function is called with a fresh registry, exactly the two tools named in `example/lib/example_tools.dart` shall be registered, and invoking the increment tool shall raise the example's counter by one. | Run the test stage; `example/test/example_tools_test.dart` asserts the two registered names and the counter value before and after invocation. | Rename one of the two tools in `example/lib/example_tools.dart` to a different string without updating the test. Re-run the test stage: the registered-names assertion fails, so the check fails. Restore the file. |
| AC-023 | When the build stage runs, `flutter build web` inside `example/` shall exit zero and `example/build/web/index.html` shall exist afterwards. | Run the build stage on the unmodified tree; read the exit status and check that the file exists. | Delete `example/web/index.html`. Re-run the build stage: the build exits non-zero reporting the missing web entry file and the output file is not produced, so the check fails. Restore the deleted file. |
| AC-024 | When `.github/workflows/checks.yml` is read, it shall define exactly two jobs, one for the wiki lint and one for the package check suite, and neither job definition shall declare a dependency on the other. | Parse or read the workflow file, list the job keys, and confirm that no job definition contains a dependency key. This check needs no push. | Add a dependency key naming the wiki job to the package job definition. Re-read the file: a dependency key is now present, so the check fails. Remove the key. |
| AC-025 | When the package job in `.github/workflows/checks.yml` is read, exactly one of its steps shall run the literal command `bash tools/check.sh`, and `AGENTS.md`'s `## Checks` section shall name that identical literal as the full suite. | Read the command string from the workflow step and the command string from the `## Checks` section and compare both to the fixed literal `bash tools/check.sh`. | Change the full-suite command named in `AGENTS.md`'s `## Checks` section to `sh tools/check.sh`. Compare again: it no longer matches the fixed literal, so the check fails. Restore the section. |
| AC-026 | When the workflow file is read, exactly one Flutter version literal shall appear in it, matching the shape of a real Flutter release version (three dot-separated numeric components). If, additionally, a human has explicitly authorized pushing a branch and a completed run's package-job log exists, that log's printed resolved-version line shall contain the same literal — but this second half is not required for this work item to be complete, since it depends on a push this plan cannot authorize on anyone's behalf. | Count the version literals in the workflow file and check the shape with a pattern match. If a completed run's log is available, separately read its printed version line; if none is available because the push was not authorized, record this half as pending rather than failing. | Add a second, different version literal to the package job (for example in a comment referencing an old pin). Re-count: two literals are present where the criterion requires exactly one, so the file-read half fails regardless of whether a push has happened. Remove the second literal. |
| AC-027 | When `README.md` is read, its first heading shall be a new level-one heading naming the package, the previously first line shall still be present unchanged as the second level-one heading, every pre-existing line shall be byte-identical, and the sentence naming the Cursor third-party-plugins toggle shall be unchanged. | Read the diff for `README.md` and confirm every changed line is an insertion positioned above the previously first line; search the file for the Cursor toggle sentence and compare it to the pre-change text. | Reword the Cursor third-party-plugins sentence while making the insertion. The diff now contains a modification below the insertion point and the sentence no longer matches the pre-change text, so the check fails. Restore the sentence. |
| AC-028 | When the repository root is listed on disk, the seven paths `lib/`, `example/`, `test/`, `README.md`, `CHANGELOG.md`, `analysis_options.yaml` and `pubspec.yaml` shall all exist and be tracked by git unconditionally. `LICENSE` shall additionally exist and be tracked once a human has supplied the copyright-holder string (per the plan's step 9 gate); until then, `LICENSE`'s absence is recorded as pending, not failing. | Run a file-existence check (not `git ls-files` alone, which reports the index and can miss a working-tree rename) for each of the seven paths on disk, and separately confirm each is tracked; check `LICENSE` the same way only if the human answer has arrived. | Rename `CHANGELOG.md` to `CHANGES.md` in the working tree without staging the change. Re-run the existence check: `CHANGELOG.md` no longer exists on disk even though `git ls-files` still lists it, so the check fails regardless of `LICENSE`'s state. Restore the name. |
| AC-029 | When `pubspec.yaml` is read, its `platforms` map shall contain the key `web` and no other platform key. | Read the platforms map from the manifest and list its keys. | Add an `android` key to the platforms map. Re-read the manifest: the key list now contains a platform other than `web`, so the check fails. Remove the key. |
| AC-030 | When `AGENTS.md` is read, it shall contain a `## Checks` section naming four commands — full suite, single test, format, analyze — and the file shall remain under 200 lines. | Search the file for the heading and the four command strings; count the lines; run `python3 tools/lint_wiki.py` and read its exit status. | Delete the `## Checks` heading while leaving the four command lines in place. Search again: the heading is absent, so the check fails. Restore the heading. |
| AC-034 | When one descriptor is added to a `WebMcpScope` directly and a second is added through a `WebMcpToolSource` passed to that scope's `addSource`, on a registry with no other registrations, `WebMcp.instance.tools` shall list exactly those two names and the scope's owned-name list shall equal the same two names sorted ascending; when that scope is then closed, `WebMcp.instance.tools` shall be empty and the scope's owned-name list shall be empty. | Run the test stage; `test/webmcp_scope_test.dart` resets the registry, adds one tool directly and one through a source, asserts both listings, closes the scope and asserts both are empty. | Apply two mutations, each checked in isolation: (a) change `close` in `lib/src/webmcp_scope.dart` to clear its own owned-name list without calling the registry's `unregisterTool` — after the close, `WebMcp.instance.tools` still lists two names, so the assertion fails; (b) change `addSource` in the same file to call the registry's `registerSource` instead of routing each descriptor through `addTool` — the source's tool is registered but is absent from the scope's owned-name list and survives the close, so two assertions fail. Restore the file after each. |
| AC-035 | When a name is already registered by another owner and a scope's `addTool` is called with that name, `addTool` shall return false, the name shall appear in that scope's skipped-name list and not in its owned-name list, invoking that name shall reach the first owner's handler, and closing that scope shall leave the name registered. | Run the test stage; `test/webmcp_scope_test.dart` registers a tool whose handler returns a first marker value directly on the registry, then adds a same-named tool whose handler returns a second marker through a scope, asserts the return value of `addTool`, both name lists, the value returned by `invokeTool`, and the registry listing after that scope is closed. | Change `addTool` in `lib/src/webmcp_scope.dart` to record the name as owned before calling the registry, so a skipped name is treated as owned. Re-run the test stage: the skipped-name list is empty, the owned-name list has one entry, and closing the scope removes the first owner's tool, so three assertions fail. Restore the file. |
| AC-036 | When `close` is called twice on the same scope, the second call shall remove nothing and throw nothing; and when `addTool` or `addSource` is called on a closed scope, it shall throw `StateError` and the registry listing shall stay unchanged. | Run the test stage; `test/webmcp_scope_test.dart` closes a scope twice, asserts no throw and an unchanged empty listing, then asserts the thrown type for both add methods on the closed scope. | Delete the closed-state guard from `addTool` in `lib/src/webmcp_scope.dart`. Re-run the test stage: no `StateError` is thrown and the registry listing gains an entry after the scope was closed, so both assertions fail. Restore the file. |
| AC-037 | When a scope's `addTool` is called with a name containing a space, it shall throw `WebMcpInvalidToolNameException`, the name shall appear in neither the owned-name list nor the skipped-name list, and the registry listing shall stay unchanged. | Run the test stage; `test/webmcp_scope_test.dart` asserts the thrown type and both name lists after the throw. | Change `addTool` in `lib/src/webmcp_scope.dart` to catch every `WebMcpException` rather than only the duplicate one. Re-run the test stage: nothing is thrown and the invalid name is recorded as skipped, so the thrown-type assertion fails. Restore the file. |
| AC-038 | When a `State` mixing in `WebMcpScreen` whose `registerWebMcpTools` override adds one tool is pumped into a widget tree, `WebMcp.instance.tools` shall list exactly that one name while the widget is mounted; when that widget is pumped away, `WebMcp.instance.tools` shall be empty. | Run the test stage; `test/widget_layer_test.dart` pumps a host screen defined in that file, asserts the listing, pumps a different widget in its place, and asserts the empty listing. | Delete the `dispose` override from the mixin in `lib/src/widgets/webmcp_screen.dart`. Re-run the test stage: after the screen is pumped away the listing still has one entry, so the assertion fails. Restore the file. |
| AC-039 | When a `WebMcpAction` with a given name is mounted inside a screen using the `WebMcpScreen` mixin, `WebMcp.instance.tools` shall list that name character for character with no prefix or suffix; when the action alone is removed from the screen's build while the screen stays mounted, `WebMcp.instance.tools` shall no longer list that name. | Run the test stage; `test/widget_layer_test.dart` pumps the host screen with the action present, asserts the exact listed name, rebuilds the host with the action omitted, and asserts the name is gone while the screen is still mounted. | Change `WebMcpAction`'s state in `lib/src/widgets/webmcp_action.dart` to register a name built from the scope name and the widget name joined by a separator. Re-run the test stage: the listed name no longer equals the literal passed to the constructor, so the assertion fails. Restore the file. |
| AC-040 | When a `WebMcpAction` wraps a button, the button shall be findable in the pumped tree, tapping it shall run the button's own callback exactly once, and the single element directly below the wrapper's element shall hold the same widget instance that was passed as `child`. | Run the test stage; `test/widget_layer_test.dart` pumps the action around a button whose callback increments a counter held in the test file, finds the button by type, taps it, asserts the counter is one, and walks the wrapper's element to assert its only child element's widget is identical to the child instance passed in. | Change `WebMcpAction`'s `build` in `lib/src/widgets/webmcp_action.dart` to return an absorbing pointer wrapper around the child. Re-run the test stage: the tap no longer reaches the button and the counter stays zero, so the assertion fails. Restore the file. |
| AC-041 | When a mounted `WebMcpAction` is rebuilt with a different `onInvoke` closure and the tool is then invoked, `invokeTool` shall complete with the value produced by the closure supplied at the most recent rebuild. | Run the test stage; `test/widget_layer_test.dart` pumps the action with a closure returning a first marker, invokes and asserts it, rebuilds with a closure returning a second marker, invokes again and asserts the second marker. | Change `WebMcpAction`'s state in `lib/src/widgets/webmcp_action.dart` to register the widget's `onInvoke` reference captured at registration instead of a forwarder that reads the current widget. Re-run the test stage: the second invocation returns the first marker, so the assertion fails. Restore the file. |
| AC-042 | When a `WebMcpAction` is mounted with no ancestor `State` mixing in `WebMcpScreen`, the pump shall surface a `WebMcpScopeMissingException` whose string form contains that action's tool name, and `WebMcp.instance.tools` shall be empty. | Run the test stage; `test/widget_layer_test.dart` pumps the action with no host screen above it, asserts the thrown type and that the tool name appears in the exception's string form, and asserts the empty listing. | Change `WebMcpAction`'s state in `lib/src/widgets/webmcp_action.dart` to fall back to registering directly on `WebMcp.instance` when no scope is found. Re-run the test stage: nothing is thrown and the listing has one entry, so both assertions fail. Restore the file. |
| AC-043 | When two screens are mounted at the same time and each contains a `WebMcpAction` with the same tool name, `WebMcp.instance.tools` shall list that name exactly once, invoking it shall reach the first-mounted action's callback, the second screen's scope shall report that name as skipped, and unmounting the second screen shall leave the name registered and still reaching the first action's callback. | Run the test stage; `test/widget_layer_test.dart` pumps both host screens in one tree, asserts the listing length, invokes and asserts the marker value, unmounts the second screen, then invokes and asserts the same marker again. | Delete the duplicate-exception catch from `addTool` in `lib/src/webmcp_scope.dart` so the exception propagates out of the second action's registration. Re-run the test stage: pumping the two screens throws `WebMcpDuplicateToolException` and the test fails before its first assertion. Restore the file. |
| AC-044 | When any Dart file under `lib/` whose path is not inside `lib/src/widgets/` contains an import directive whose URI begins with `package:flutter/`, the test stage shall fail and name that file and that import. | Run the test stage on the unmodified tree; the Flutter-import scan in `test/repo_hygiene_test.dart` passes and reports zero offending files while the two files under `lib/src/widgets/` do import `package:flutter/widgets.dart`. | Add an import of `package:flutter/widgets.dart` to `lib/src/webmcp.dart`. Re-run the test stage: the scan fails naming that file and that import, so the check fails. Remove the import. |
| AC-045 | When `test/webmcp_scope_test.dart` is read, it shall contain no import from `package:flutter_test/` and no call that pumps a widget tree, and the test stage shall still exercise the scope's add, skip, remove and close behaviour from that file. | Read the file's import lines and search it for a pump call; run the test stage and confirm the scope tests execute. | Add a `package:flutter_test/` import and one pumped-widget test to `test/webmcp_scope_test.dart`. Re-run the test stage: the hygiene test's scan of that specific file reports the forbidden import and fails, even though the added test itself passes. Remove the import and the added test. |
| AC-046 | When `example/lib/example_screen.dart` is read, it shall declare a `State` subclass mixing in `WebMcpScreen` and shall contain at least one `WebMcpAction`, and `example/lib/main.dart` shall import and mount that screen. | Read both files for the mixin clause, the wrapper's type name and the import; run the analysis and build stages and confirm both exit zero. | Delete `example/lib/example_screen.dart`. Re-run the analysis stage: it exits non-zero reporting an unresolved import and an undefined class name in `example/lib/main.dart`, so the check fails. Restore the file. |

## Non-functional criteria

| ID | Criterion | How it is checked | Negative case |
|---|---|---|---|
| AC-031 | When `tools/check.sh` is run on a warm checkout with dependencies already resolved, the whole run shall complete in under five minutes of wall-clock time. | Run the script under a timing command twice in a row and read the elapsed time of the second run. | Insert a stage into the script that sleeps for four hundred seconds. Re-run under the timing command: the elapsed time exceeds the five-minute ceiling, so the check fails. Remove the inserted stage. |
| AC-032 | When any tracked file (any extension, not only `.dart`) under `lib/`, `example/lib/`, `tools/` or `.github/` contains one of the fixed secret markers listed in the plan's interfaces section, the test stage shall fail and name the file and the marker. | Run the test stage on the unmodified tree; the secret-scan group in `test/repo_hygiene_test.dart` passes and reports zero matches across all four directories regardless of file extension. | Add a line containing a synthetic AWS-access-key-prefix string to `.github/workflows/checks.yml` (a YAML file, not Dart) — a scan restricted to Dart sources would miss this. Re-run the test stage: the scan fails naming that file and marker, so the check fails. Remove the line. |
| AC-033 | When `.gitignore` is read, it shall contain a pattern matching the root and example `build/` directories, both `.dart_tool/` directories, and both `pubspec.lock` paths. | For each of the six paths, run `git check-ignore -v` against a sample path under it and confirm a match naming the expected `.gitignore` line. | Delete the pattern covering `example/build/` from `.gitignore`. Re-run `git check-ignore -v example/build/web/x.dart`: it reports no match, so the check fails. Restore the pattern. |

## Explicitly not required

- Any call to the browser's WebMCP API, including registration, discovery, execution, the change event and abort-signal unregistration. The web transport only detects and logs in this work item.
- Behaviour in a real browser. No criterion here is checked by running the example in Chrome; the example is only required to build and to register its tools in a virtual-machine test, and the web transport's source text is checked without executing it (AC-021).
- A behavioural test of the example's screen. The widget-lifecycle layer's behaviour is covered by the root suite (AC-038 to AC-043); the example screen is covered only by the format, analysis and build stages plus the structural check in AC-046.
- Any automatic discovery of widgets to expose. No criterion requires a tool to appear without a `WebMcpAction` or a `registerWebMcpTools` override naming it in source.
- An `enabled` or otherwise conditional-exposure parameter on `WebMcpAction`, and any registration path driven by a widget update rather than by mount and unmount.
- Any derivation of a tool name from a scope, screen or route. Names are registered character for character as written (AC-039).
- Navigation-tool generation from a router. Only the tool-source interface exists; no adapter for any router is required.
- A dev-time or non-browser transport implementation. Only the transport interface and the two skeleton implementations are required.
- Publishing to pub.dev, a pana score, or any comparison against the competing packages.
- A `repository` or `homepage` field in `pubspec.yaml`. Omitted deliberately in this work item; see the plan's Interfaces and Out-of-scope sections.
- Per-screen tool shadowing, typed input decoding, and output schemas. The scope introduced by the widget-lifecycle layer groups tools by lifetime and never resolves one name to different handlers by screen; the duplicate case is the skip behaviour in AC-043, not shadowing.
- Support for any platform other than web, and any lock file being committed.
- Concurrency, isolate safety, or thread safety of the registry beyond single-threaded Dart semantics.

## Verdict log

| Round | Date | Verdict | Report |
|---|---|---|---|
